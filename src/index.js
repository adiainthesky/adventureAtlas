import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// To start measuring performance in app, pass a function to log results (eg: reportWebVitals(console.log))
// or send to an analytics endpoint. More info: https://bit.ly/CRA-vitals
reportWebVitals();
